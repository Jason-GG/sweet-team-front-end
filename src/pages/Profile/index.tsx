import PlaceholderPage from '../../components/ui/PlaceholderPage'

function ProfilePage() {
  return (
    <PlaceholderPage
      eyebrow="Profile"
      title="Profile is intentionally lightweight until authentication requirements are clear."
      description="The route is present for navigation completeness. Saved booths, preferences, and account settings can be added once auth and persistence decisions are finalized."
    />
  )
}

export default ProfilePage